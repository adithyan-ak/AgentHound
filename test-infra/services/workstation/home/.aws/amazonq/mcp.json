{
  "mcpServers": {
    "amazon-q-fixture": {
      "command": "/usr/bin/printf",
      "args": ["fixture amazon q"],
      "env": {
        "AWS_PROFILE": "fixture-profile",
        "AWS_REGION": "us-east-1"
      }
    }
  }
}
