# Pinned to the versions verified against the collector's wire contracts on
# 2026-07-14 (Python 3.12). The low-level `Server` + `StreamableHTTPSessionManager`
# API used by app.py is current as of mcp 1.28.1.
#
# httpx is a runtime dependency of `mcp` and is also imported by test_app.py.
mcp==1.28.1
starlette==1.3.1
uvicorn==0.51.0
httpx==0.28.1
